node_modules
dist
.DS_Store
*.local
.vite

# Royal Enfield model is fetched locally via `npm run fetch-model` and intentionally not
# committed (see README "asset provenance"). Rider GLBs (CC0-derived) are committed.
public/models/scram411.glb
